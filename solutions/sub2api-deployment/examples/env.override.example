# Copy only the values you actually want to override into your generated .env.
# Do not commit the real file that contains secrets.

ADMIN_EMAIL=admin@example.com
ADMIN_PASSWORD=replace_me_before_first_login
SERVER_PORT=8080
TZ=Asia/Shanghai
